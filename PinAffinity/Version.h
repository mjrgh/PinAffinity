#pragma once

// Major and minor version number.  When displaying the
// version, we also add a generated build number, so we
// don't bother with a third dotted part; the build number
// serves that role more effectively for an open source
// project like this that doesn't have a formal release
// cycle.
#define VSN_MAJOR   1
#define VSN_MINOR   0
